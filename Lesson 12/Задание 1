def product(a, b):
    if a < b:
        return product(b, a)
    elif b != 0:
        return a + product(a, b - 1)
    else:
        return 0


a = len((input()))
b = len((input()))

print(product(a, b))
