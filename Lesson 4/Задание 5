num = int(input())
hours = num // 3600
minutes = num % 3600 // 60
seconds = num % 60
print(str(num) + ' ' + 'секунд' + ' ' + 'это' + ' ' + str(hours) + ' ' + 'час' + ' ' + str(minutes) + ' ' + 'мин'
      + ' ' + str(seconds) + ' ' + 'сек')
